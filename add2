#include <stdio.h>
#include "myBigChars.h"

// Функция для печати результата теста
void print_test_result(char* test_string, int expected) {
    int result = bc_strlen(test_string);
    printf("Тестирование строки: %s\nОжидаемая длина: %d, Полученная длина: %d\n",
           test_string, expected, result);
}

int main() {
    // Тест с корректной однобайтной строкой
    print_test_result("Hello", 5);

    // Тест с UTF-8 строками
    print_test_result("\xC2\xA2", 1); // символ ¢ (цент)
    print_test_result("\xE2\x82\xAC", 1); // символ € (евро)
    print_test_result("\xF0\x9F\x92\xA9", 1); // символ 💩 (пупс)

    // Тесты с некорректными UTF-8 последовательностями
    print_test_result("\xC2\x20", 0); // невалидное продолжение
    print_test_result("\xE2\x82\x20", 0); // невалидное продолжение
    print_test_result("\xF0\x9F\x20\xA9", 0); // невалидное продолжение

    return 0;
}

        // Выводим номер итерации
        cout << "Итерация " << iter << ":" << endl;
        for (size_t i = 0; i < x.size(); ++i) {
            cout << "x[" << i << "] = " << x[i] << endl;
        }
